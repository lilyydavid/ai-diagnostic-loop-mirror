# Onboarding a new team or space

Describe steps to configure permissions and settings for a new Claude team or workspace.

- Add team members to `settings.json` with appropriate roles.
- Update `skills/` subfolders with new agent definition if needed.
- Fill out `.mcp.json` with project-scope MCP configuration (kept git-ignored).
